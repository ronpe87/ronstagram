= form_with(model: @comment, url: photo_comments_path(@comment.photo), local: true) do |f|
  %div
    = f.label :content, '内容'
  %div
    = f.text_area :content
  = f.submit '保存', class: 'btn-primary'
